#Full Stack Development

fornt end + backend + data base=full Stack Development

     *)forntend  --ux/ui design which users can seen and experiance 
     *)backend   --operation that are proccessing which can not be seen by users
     *)database  --storing the data from the backend is now as DB(collect of informations)


    
